{
	host: {
		# server port in host machine
		port: "8082"
		port: ${?PORT}
	}

	worker: {
		# worker host url
		host: "localhost"
		host: ${?WORKER_HOST}

		# worker post in host machine
		port: "8081"
		port: ${?WORKER_PORT}
	}
}
