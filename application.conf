{
	host: {
		# server port in host machine
		port: "8002"
		port: ${?PORT}
	}

	worker: {
		# worker host url
		host: "localhost"
		host: ${?WORKER_HOST}

		# worker post in host machine
		port: "8001"
		port: ${?WORKER_PORT}
	}
}
